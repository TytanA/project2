<%- include('../partials/header') %>
<h1> this is index page</h1>

<% campaigns.forEach(function(c) { %>
    <tr>
        <td><%= c.titleName %> </td>
    </tr>

<% }) %>
<%- include('../partials/footer') %> 